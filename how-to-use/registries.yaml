# /etc/ranchers/k3s/ 에 이 파일을 위치시키면 private registry 등록 가능
mirrors:
  docker.wai:
    endpoint:
      - "http://docker.wai"
configs:
  "docker.wai":
    auth:
      username: xxxxxx # this is the registry username
      password: xxxxxx # this is the registry password